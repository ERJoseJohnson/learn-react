
/* 
This is a simple definition of a custom component.

A component is basically a function that returns some HTML code for react to render on a HTML document.
Remember to export your component so that your root App.js file can import this component
*/
function Item(params) {
    return <h2>This is an item!</h2>
}

export default Item;